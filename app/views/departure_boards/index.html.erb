<p id="notice"><%= notice %></p>

<h1>Departure Boards</h1>

<table>
  <thead>
  <tr>
    <th>Station</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
    <% @origins.sort.each do |origin| %>
      <tr>
        <td><%= origin %></td>
        <td><%= link_to 'Show Departures', "#{departure_boards_url}/#{origin}" %></td>
      </tr>
    <% end %>
  </tbody>
</table>


